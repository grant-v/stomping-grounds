// Copy this file to config.js and paste your own Google Maps API key between the quotes
// to turn on Google's photorealistic 3D spot view. Leave it empty to use the built-in
// (keyless) 3D view, which needs no account and works out of the box.
//
// In Google Cloud: enable "Map Tiles API" and "Maps JavaScript API" for the key, then
// restrict it to the HTTP referrers you actually use, e.g.
//   http://localhost:8377/*
//   https://grant-v.github.io/*
// config.js is listed in .gitignore so your key stays on your machine.
window.MYWAR_CONFIG = {
  googleMapsKey: ''
};
